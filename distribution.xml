<?xml version="1.0" encoding="UTF-8"?>
<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3
                              http://maven.apache.org/xsd/assembly-1.1.3.xsd">

    <id>distribution</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>

    <files>
        <!-- JARs with version-stripped names -->
        <file>
            <source>${project.build.directory}/${project.artifactId}-${project.version}.jar</source>
            <outputDirectory>lib</outputDirectory>
            <destName>${project.artifactId}.jar</destName>
        </file>
        <file>
            <source>${project.build.directory}/${project.artifactId}-${project.version}-bundled.jar</source>
            <outputDirectory>lib</outputDirectory>
            <destName>${project.artifactId}-bundled.jar</destName>
        </file>
    </files>

    <fileSets>

        <!-- Root files -->
        <fileSet>
            <directory>${project.basedir}</directory>
            <outputDirectory>.</outputDirectory>
            <includes>
                <include>LICENSE</include>
                <include>README.md</include>
            </includes>
        </fileSet>

        <!-- utils/bin scripts (make them executable) -->
        <fileSet>
            <directory>${project.basedir}/src/main/distribution/utils/bin</directory>
            <outputDirectory>bin</outputDirectory>
            <includes>
                <include>**/*</include>
            </includes>
            <fileMode>0755</fileMode>
        </fileSet>

        <!-- Example themes -->
        <fileSet>
            <directory>${project.basedir}/examples</directory>
            <outputDirectory>examples</outputDirectory>
            <includes>
                <include>**/*</include>
            </includes>
            <fileMode>0644</fileMode>
        </fileSet>

    </fileSets>
</assembly>
